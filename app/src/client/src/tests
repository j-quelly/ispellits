// dependencies
import React from 'react';
import ReactDOM from 'react-dom';
import { shallow } from 'enzyme';

// components
import App from '../App';
import { Header } from '../Header';
import Body from '../Body';
// import Engine from '../Engine';
// import Modal from '../Modal';
// import Footer from '../Footer';

  describe('<Header />', () => {

    it('should display a modal', () => {
      expect(
        wrapper.find(Header).at(0).props().modal
      ).toBe(true);
    });

    it('should have one lives', () => {
      expect(
        wrapper.find(Header).at(0).props().lives
      ).toBe(1);
    });

    it('should have no points', () => {
      expect(
        wrapper.find(Header).at(0).props().score
      ).toBe(0);
    });

    it('should have a logo component', () => {
      expect(
        wrapper
          .find(<Logo logo="path/to/file" />)
          .childAt(0)
      ).toBe(true);
    });

  });